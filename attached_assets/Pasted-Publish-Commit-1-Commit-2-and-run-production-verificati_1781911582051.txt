Publish Commit 1 + Commit 2 and run production verification.

Do not start Commit 3 yet.
Do not delete email_filters rows yet.
Do not drop the email_filters table.
Do not remove backend email_filters routes yet.

Verify in production:

1. Inbox visible unread behavior

* Log in as Trevor.
* Select trevor@voltsafe.com.
* Open Inbox > Unread > Smart Inbox.
* Confirm previously hidden LinkedIn/Substack/Beehiiv/etc. unread messages now appear in the inbox.
* Confirm the inbox no longer drops from ~50 API rows to 2 visible rows because of domain filtering.

2. Pipeline proof
    Capture:

* /api/gmail/messages?q=in:inbox is:unread response count
* allInboxMessages count
* inboxMainRaw count
* inboxMain count
* rendered row count

Expected:

* inboxMainRaw should no longer drop blocked-domain messages.
* No blockedDomains filter should run.

3. Spam behavior

* Spam view should show only real spam from spamQuery.data.messages.
* No “Other” overlay.
* No domain-blocked inbox messages mixed into Spam.

4. Exact sender block behavior
    Using a safe dev/test message first, then production only if safe:

* Block exact sender.
* Confirm message moves out of Inbox immediately.
* Confirm is_inbox=false.
* Confirm is_spam=true.
* Confirm is_unread unchanged.
* Confirm message appears in Spam.

5. Trust / Shield behavior

* Trust the same sender/message.
* Confirm message returns to Inbox.
* Confirm is_inbox=true.
* Confirm is_spam=false.
* Confirm is_unread unchanged.
* Confirm exact sender removed from blocked_senders.
* Confirm spam_trusted_senders contains sender.

6. UI cleanup
    Confirm:

* Other tab is gone from visible navigation.
* “Block entire domain” action is gone.
* Exact sender Block action remains.
* No console errors.

7. Tests
    Run:

* mark-spam-derived-columns.test.cjs
* block-sender.test.cjs
* block-sender-api.test.cjs
* inbox-count-reconciliation.test.cjs
* inbox-category-visibility.test.cjs
* smart-inbox-phase6b.test.cjs

Return:

* production screenshots
* before/after visible unread counts
* test results
* any regressions
* recommendation on whether Commit 3 is necessary