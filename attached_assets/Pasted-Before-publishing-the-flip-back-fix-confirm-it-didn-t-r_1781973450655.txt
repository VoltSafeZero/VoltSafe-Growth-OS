Before publishing the flip-back fix: confirm it didn't reintroduce the badge-lag bug or mask a server-write failure. Audit/verify only — no new code unless I approve.
Three fixes have now touched handleSelectMessage's read path in sequence: (1) Bug C added invalidateQueries(["/api/gmail/messages"]) to make the badge decrement immediately; (2) this flip-back fix removed that exact line and replaced it with re-applying the optimistic patch. I need to confirm these compose correctly, not just that the flip-back is gone.

Did removing the message-list invalidation reintroduce Bug C? Bug C was "badge doesn't decrement on click." The flip-back fix removed the invalidateQueries(["/api/gmail/messages"]) that was added to fix Bug C, keeping only invalidateBadgeQueries(). Confirm precisely: after a mark-read click, does invalidateBadgeQueries() alone cause /api/gmail/accounts/health and /api/gmail/category-counts to refetch and the badge to decrement immediately? Or did the badge only decrement before because of the now-removed message-list invalidation? Trace what invalidateBadgeQueries() actually invalidates and confirm it covers the badge queries independently of the message-list query.
Is the server-side is_unread=false write actually landing, or is the client just hiding the race? The flip-back root cause was: the refetch returns the message still tagged UNREAD because the server write hadn't committed. The fix makes the client re-assert its optimistic patch instead of trusting that refetch. But that only masks a slow/failed server write. Confirm: when does email_messages.is_unread actually become false for a clicked message — synchronously before the route returns 200, or eventually? And critically: is that DB write gated behind the markMessageRead Gmail API call succeeding (the concern from the prior OAuth audit)? If Gmail 503's, does is_unread=false still get written locally, or does the client now show "read" while the server permanently says "unread" — which would make the badge wrong on next refetch and recreate the original stuck-count bug?
Test the compose explicitly: click an unread PEOPLE message and verify ALL of: (a) row clears immediately, (b) row stays read through 3s/15s/refresh — the flip-back fix, (c) the badge decrements immediately — Bug C still fixed, (d) the DB row actually has is_unread=false after the click. All four must hold together. If any one fails, report which and stop.

Report findings. Recommend whether the server write needs to be made resilient (persist is_unread=false locally even if Gmail throws) so the client and server can't disagree. Do not implement that without my approval.
